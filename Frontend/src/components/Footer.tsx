'use client'

// STUB BCaaS — Pied de page
// Ce composant est un stub non fonctionnel décrit dans le rapport.

import { type ReactElement } from 'react'

export default function Footer(): ReactElement {
  return (
    <footer style={{
      padding: '1rem 2rem',
      borderTop: '1px solid rgba(42,32,25,0.11)',
      textAlign: 'center',
      color: '#7b6b5d',
      fontSize: '0.72rem',
    }}>
      Footer — Stub non fonctionnel
    </footer>
  )
}