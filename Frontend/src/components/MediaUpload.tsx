'use client'

// STUB BCaaS — Upload d'images
// Ce composant est un stub non fonctionnel décrit dans le rapport.
// Il devait valider le type MIME et la taille (max 10 Mo, formats JPG/PNG/WebP).

import { type ReactElement } from 'react'

export default function MediaUpload(): ReactElement {
  return (
    <div style={{ padding: '1rem', border: '1px dashed #9d6a3b', color: '#7b6b5d', fontFamily: 'DM Mono, monospace', fontSize: '0.75rem' }}>
      📸 MediaUpload — Stub non fonctionnel
    </div>
  )
}